<%- include('../partials/heading') -%>
    <div class="text-center ">
        <div class="container border-secondary border-left border-right  rounded border-2 p-5 mx-auto my-5 bg-white  ">
            <h2 class="my-3 display-4 m-5">
                <%= post.title %>
            </h2>
            <img class="img-thumbnail" src="<%=post.image%>" alt="img item">

            <p class="my-3 ">
                <%- post.text %>
            </p>
            <%if(currentuser && currentuser.role =="administrator" ){ %>
                <div class="text-center">
                    <a style="margin: 0px auto;" href="/posts/<%=post._id%>/edit" class="btn btn-light text-warning">Update</a>
                    <form class="del-form" action="/posts/<%=post._id%>?_method=DELETE" , method="POST">
                        <button type="submit" class="btn btn-light text-danger">Delete</button>
                    </form>
                </div>
                <% } %>

                    <div style="padding-bottom:100px" class="container  justify-content-center mt-5 border-left border-right bg-light text-center rounded">
                        <% if(currentuser){ %>
                            <form action="/posts/<%=post._id%>/comments" , method="POST">

                                <div class="d-flex justify-content-center pt-3 pb-2"> <input type="text" name="comment[text]" placeholder="+ Add a comment" class="form-control addtxt"> </div>

                            </form>
                            <%}%>

                                <div class=" py-2 px-2">
                                    <% post.comments.forEach(function(comment){ %>

                                        <div class="container">
                                            <div class="border-top border-dark border-3 d-flex flex-row justify-content-between m-3">
                                                <div id="commentcontainer" class="mt-5">
                                                    <%= comment.text %>
                                                </div>
                                                <div class="font-weight-bold mb-4 border-bottom bordr-light  ">
                                                    <i> <img class="rounded-circle d-block " src=" <%= comment.author.image %>" alt="user"> 
                                                        <%= comment.author.username %> 
                                                    </i>
                                                </div>

                                            </div>
                                        </div>

                                        <div class=" left ">
                                            <% if( currentuser ){ %>
                                                <% var userid = currentuser._id.toString(), commentauthor = comment.author.id.toString() %>
                                                    <% if(userid === commentauthor || currentuser.role == "administrator"){ %>
                                                        <small> <a href="/posts/<%=post._id%>/comments/<%=comment._id%>/edit" class="btn btn-light text-warning btn-sm">update</a></small>
                                                        <form class="del-upd-form" action="/posts/<%=post._id%>/comments/<%=comment._id%>?_method=DELETE" method="POST">
                                                            <small> <button class="btn btn-light text-danger btn-sm">Delete</button></small>
                                                        </form>
                                                        <span class="thumbup btn btn-light text-primary">
                                                            <i class="far fa-thumbs-up"></i> 3 </span>
                                                        <%}else{%>
                                                            <span class="thumbup btn btn-light text-primary">
                                                        <i class="far fa-thumbs-up"></i> 3 </span>

                                                            <%}}else{%>
                                                                <span class="thumbup btn btn-light text-primary">
                                                                        <i class="far fa-thumbs-up"></i> 3 </span>
                                                                <%}%>
                                        </div>
                                        <% }) %>
                                </div>
                    </div>
                    <div class="text-center">
                        <a style="margin: 0px auto;" href="/posts/<%=post._id%>/comments/new" class="btn btn-dark my-4 ">Add a comment</a>
                    </div>
        </div>

        <div class="text-center">
            <a href="/" class="btn btn-light my-3">Back to main page</a>
        </div>
    </div>


    <%- include( '../partials/footer') -%>